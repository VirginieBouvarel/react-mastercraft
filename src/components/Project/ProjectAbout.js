import classes from './ProjectAbout.module.css';
import Card from './../UI/Card';


const ProjectAbout = (props) => {
  return (
    <Card className={classes.classe}>

    </Card>
  )
}

export default ProjectAbout